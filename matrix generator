import random
def matrices():
    r, c = random.randint(0,9), random.randint(0,9)
    matrix = []
    for i in range(r):
        a = []
        for j in range(c):
            a.append(random.randint(0,9))
        matrix.append(a)
    return matrix

A=[]
A=matrices()
for r in A:
	print(r)
