num_tests = int(input())
results = []
for i in range(num_tests):
    x, y, k = map(int, input().split())
    result = "Yes" if x % k == 0 and y % k == 0 else "NO"
    results.append(result)
print('\n'.join(results))
